# ---------- 构建产物 ----------
# OutputPath / BaseIntermediateOutputPath 现在指向 build\（见 csproj 与
# Directory.Build.props），所以 src 下不该再出现产物；bin/ obj/ 这两条留着兜住
# 别的项目的默认布局。build\obj 已被 obj/ 覆盖，build\Release 得单独写：
build/Release/
build/Debug/
bin/
obj/
*.user
*.suo
.vs/

# 图标预览图，跑 build/make-icon.ps1 时会重新生成，不必入库
build/icon-preview.png

# ---------- 运行时数据 ----------
# 内含服务器地址、账号与加密后的口令，以及历史采样，绝不入库
data/

# ---------- 第三方二进制（体积大，改为按需下载） ----------
# 见 tools/README.md 里的下载地址
# 故意不写 tools/ 前缀：部署前常把整个 tools 目录复制/改名成别的文件夹，
# 那些副本里的 OpenSSH-Win64 一样是 11MB 二进制，同样不该入库
OpenSSH-Win64/

# ---------- 编辑器 / 系统 ----------
.idea/
.vscode/
.ccgui/
Thumbs.db
desktop.ini
*.tmp
*.log
